pymongo[srv]>=4.0,<5.0
python-dotenv>=1.0.0

# claude_proxy feature (token-aware context trim) — required if you run
# install_claude_proxy.py. install_claude_proxy.py preflight-checks these.
httpx>=0.27
fastapi>=0.110
uvicorn>=0.27
